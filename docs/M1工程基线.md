# 羽来 M1 工程基线

## 完成范围

- Git 仓库和 main 分支
- pnpm 单仓库工作区
- 原生 TypeScript 微信小程序目录
- TypeScript 模块化 `api` 云函数
- `health.get` 健康检查
- `auth.session` 可信身份和用户初始化
- 小程序统一云函数调用层、请求 ID 和错误处理
- CloudBase CLI 动态环境配置和部署命令
- OPENID HMAC 存储方案，密钥只存在云函数环境变量
- 用户、学校、场馆、场地、球局、报名、参与者和审计集合定义
- 试点场馆种子数据
- 8 个集合均限制为仅管理端和云函数可读写
- CI、格式检查、静态分析、类型检查、单元测试和构建

## 验收结果

2026年9月8日本地验证通过：

- Biome 格式检查通过
- Biome 静态检查通过
- 小程序、共享契约和云函数 TypeScript 检查通过
- 云函数 7 个单元测试通过，包含公开健康检查和需登录路由的真实入口管线
- M1 基线脚本通过
- 云函数编译成功，入口为 `cloudfunctions/api/dist/index.js`

## 云端验收结果

- 已登录并核验开发环境 `cloud1-d1g8z3590d3768cbb`。
- `api` 云函数已部署，云端 `health.get` 验证通过。
- 8 个集合和全部规划索引已创建；集合权限回读结果均为 `ADMINONLY`。
- 试点场馆记录已创建，地址和场地资料仍待现场核验。
- 微信开发者工具已完成两次真实会话初始化，用户集合始终只有同一条 `ACTIVE` 记录。

微信开发者工具已重新打开项目并触发编译，首页健康检查和真实微信身份初始化均已贯通。
